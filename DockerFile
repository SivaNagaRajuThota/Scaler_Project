FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# OpenEnv usually communicates over port 8000
EXPOSE 8000

CMD ["python", "inference.py"]
